apiVersion: v1
kind: Service
metadata:
  name: midstall-vista
  namespace: midstall
spec:
  selector:
    app: midstall-vista
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
